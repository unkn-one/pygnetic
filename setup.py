from distutils.core import setup

setup(
    name='PygameNetwork',
    version='0.1dev',
    packages=['pygame_network',],
    license=open('LICENSE.txt').readline().strip(),
    long_description=open('README.txt').read(),
)